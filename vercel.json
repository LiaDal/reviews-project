{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "../../build"
        }
    ],
    "outputDirectory": "./src/client",
    "routes": [
        {
            "src": "/[^.]+",
            "dest": "/",
            "status": 200
        }
    ]
}